// För att underlätta navigationen använde jag mig av history.objectet.
// Skapade en ny komponent för att ha lättare tillgång till den

import { createBrowserHistory } from "history";
export default createBrowserHistory();
